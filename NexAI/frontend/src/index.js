import React from 'react';
import ReactDOM from 'react-dom/client';
import PatientRegistration from './components/NexAI.jsx';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <PatientRegistration />
  </React.StrictMode>
);